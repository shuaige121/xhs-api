{
  "name": "xhs-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute xhs-db --file=schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
